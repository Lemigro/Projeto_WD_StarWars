<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="#FF6F00" />  <!-- Cor quando pressionado -->
            <corners android:radius="30dp" />  <!-- Borda arredondada -->
            <stroke android:width="2dp" android:color="#000000" />  <!-- Borda preta -->
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#000000" />  <!-- Cor padrão -->
            <corners android:radius="30dp" />  <!-- Borda arredondada -->
            <stroke android:width="2dp" android:color="#FFC107" />  <!-- Borda amarela -->
        </shape>
    </item>
</selector>

